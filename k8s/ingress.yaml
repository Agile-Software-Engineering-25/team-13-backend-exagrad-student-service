apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ase-exagrad-student-service
  labels:
    app: exagrad-student-service
  annotations:
    traefik.ingress.kubernetes.io/rule: "PathPrefixStrip:`/exagrad/student`"
spec:
  ingressClassName: traefik
  rules:
    - host: sau-portal.de
      http:
        paths:
          - path: /exagrad/student
            pathType: Prefix
            backend:
              service:
                name: exagrad-student-service
                port:
                  number: 8080
